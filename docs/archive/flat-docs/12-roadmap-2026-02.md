# Roadmap 2026-02: Payments + Growth Funnel + Docs

Обновлено: `2026-02-16`

> Исторический снимок.
> В этом документе могут встречаться старые продуктовые названия `Start / Pro / Ultra`.
> Текущая живая тарифная сетка проекта: `Приветственный 30 дней (99 ₽, один раз)`, далее `1 / 3 / 6 / 9 / 12 месяцев`, где `12 месяцев = 1644 ₽` и скидка около `45%`.
> Для актуальных правил и текстов ориентируйтесь на `docs/FINAL_REPORT_RU.md`, `docs/ADMIN_FULL_GUIDE_RU.md` и живой `plan_catalog`.

## Цель релиза

За один цикл закрываем единый пакет:
- предсказуемый backend-first checkout в рублях,
- рабочие скидки для `RUB + Stars`,
- DB-backed админ-управление тарифами и Live Updates,
- обновлённый UX checkout/landing/WebApp,
- copy-pack для воронки,
- синхронизированные runbook и operator docs.

## Locked Decisions (фикс)

1. Первый релиз: полный пакет, не только docs.
2. Модель сайта: backend order first.
3. Guest без Telegram binding: ведём в бота.
4. Скидки: RUB + Stars.
5. Новости на сайте: admin-managed в БД.
6. Freekassa методы: `notify=POST`, `success/fail=GET`.
7. Политика Start/free: `Start 99 (NL, 1 device), free без дополнительного ужесточения`.
8. Цены: админ-управление в БД + формула экономики.
9. Deep links: platform-specific.
10. Публичный tone: консервативный, без абсолютных обещаний.
11. Post-payments roadmap: Retention + UI + Ops параллельно.
12. Секреты кассы: без ротации в этом цикле (принятый риск).

## Status По Потокам

### 1) Backend + Data

`Done`
- Добавлены DB сущности и миграции:
  - `plan_catalog`;
  - `live_updates`;
  - `users.pending_discount_*`.
- Промо-логика унифицирована:
  - `days` -> мгновенное продление;
  - `discount` -> pending скидка на следующий checkout.
- Реализован checkout ticket (подписанный, короткоживущий).
- Добавлен `POST /api/payments/freekassa/orders/create-public`.
- В `external_orders.meta_json` пишется breakdown (`base/discount/final`).
- Добавлена обратная совместимость:
  - если `plan_catalog` пуст, используются legacy константы.

### 2) API + Admin Interfaces

`Done`
- Public endpoints:
  - `GET /api/public/plans`;
  - `GET /api/public/live-updates`.
- Admin endpoints:
  - `GET/POST/PATCH/DELETE /api/admin/plans`;
  - `GET/POST/PATCH/DELETE /api/admin/live-updates`;
  - `GET/POST/PATCH/DELETE /api/admin/start-links`;
  - `GET/PUT /api/admin/wheel-config`;
  - `POST /api/admin/users/{telegram_id}/manual-extend` (`delta_days` + backward compatibility);
  - `POST /api/admin/campaign-links/build`.
- Freekassa callbacks:
  - `POST|GET /api/payments/freekassa/notify`;
  - успешный валидный POST отвечает `YES`.

### 3) WebApp UX

`Done`
- Админ-контролы удалены из WebApp (источник админ-правды — основной бот).
- User flow:
  - platform-specific connect actions (Android/iOS/Desktop);
  - RUB breakdown в статусе оплаты (`base/discount/final`).
  - отображение устройств: `занято X из Y` + fallback `данные недоступны`.

### 4) Marketing Site + Checkout

`Done`
- Landing Live Updates переведён на runtime API `GET /api/public/live-updates` с fallback.
- Checkout переведён на backend-first:
  - если есть `checkout_ticket` -> create-public order -> redirect на `payment_url`;
  - если ticket отсутствует -> CTA в бота.
- Freekassa widget сохранён как вспомогательный блок (по флагу/конфигу).

### 5) Docs + Operations

`Done`
- Обновлены:
  - `docs/07-payments-research.md` (payments decision note);
  - `docs/16-payments-callback-runbook.md`;
  - `ADMIN_GUIDE.md`;
  - `USER_GUIDE_RU.md`.
- Добавлены:
  - `docs/26-pricing-operations-2026-02.md`;
  - `docs/27-campaign-runbook-2min-2026-02.md`;
  - `docs/28-marketing-copy-pack-2026-02.md`;
  - `docs/29-admin-smoke-campaign-checkout-activation-2026-02.md`;
  - `docs/30-stitch-mini-handover-copy-2026-02.md`.

## Execution Order (фактический)

1. Schema + API compatibility + fallback.
2. Freekassa order-first + callback hardening.
3. Admin endpoints (plans/live updates/start-links/wheel/manual-extend).
4. Bot admin UX cleanup + WebApp user-only UX.
5. Marketing landing + checkout migration.
6. Tests + docs sync + rollout notes.

## QA Gate

`Done`:
- добавлены тесты на:
  - create-public order по checkout ticket;
  - pending discount apply/clear;
  - admin/public plans CRUD/fallback;
  - admin/public live updates CRUD/fallback.

## Next (после релиза)

1. Retention трек — `Done (2026-02-15)`:
   - worker-цепочки `welcome`, `T-3`, `T-1`, `T0`, `reactivation`;
   - A/B вариативность через детерминированный split по пользователю;
   - поддержка template override из админских `templates` (`retention_<flow>_<a|b>`).
   - дополнительный upsell-поток `start99_welcome_offer` (окно 24-48h) с автоприменением pending скидки.
2. UI-polish — `Done (2026-02-15)`:
   - усилен social proof в WebApp (отзывы, средний рейтинг, online-ноды);
   - добавлен onboarding micro-copy в статусе и connect-flow.
   - checkout получил social-proof блок в момент оплаты;
   - на лендинге добавлен публичный блок `Roadmap` и family-позиционирование `≈ ₽/чел`.
3. Referral loop — `Done (2026-02-15)`:
   - в боте добавлен soft loop `Подарить 3 дня другу` через deep-link;
   - после first paid purchase реферальный бонус по дням остаётся в силе.
4. Ops:
   - post-deploy smoke формализован в `docs/29-admin-smoke-campaign-checkout-activation-2026-02.md`.
5. Economics:
   - weekly ревизия маржинальности по Start/Pro/Ultra и правила авто-пересмотра цены.
