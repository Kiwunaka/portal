# A02 — exact owned AWG server interop

**PASS_BOUNDED / I4**, existing default-off AWG2 and AWG3.1 labs, Core
`02a091cb0e369192a5ad0909b56ccba8aa1dce17`, owned Raspberry Pi 4, direct RU
origin. A02 is verified for this declared lab scope. Android/Windows packages,
other origins, routing/MTU completeness, canary and release remain separate
requirements. [Structured receipt](evidence/a02-owned-server-20260908.json).

## Exact server and material

The actual server is a native userspace deployment, not a container. Therefore
there is no container image digest to report. Its running executable is
`amneziawg-go-v3.1.20260814`, SHA-256
`4ede58a7d4bc79bce152850ef7aa0ee5885de592d5d095352ae869b3fe8f172d`.
Both live process executables match this hash. Tools, service unit, saved
configuration and static live configuration hashes are retained independently.

The exact 3,403,924-byte server file was reproduced from
`github.com/amnezia-vpn/amneziawg-go/v3 v3.1.20260814`, origin
`1b86b2ae0e493e7ea93f8c1a0f0cb6735b1551f1`, with Go 1.25.13,
`GOOS=linux GOARCH=amd64 CGO_ENABLED=0` and:

```text
go build -trimpath -buildvcs=false -mod=readonly -ldflags "-s -w -buildid=" -o <retained-output> .
```

Fresh sumdb-authenticated downloads of the root and three compiled dependency
modules matched all 1,869 source files used by the reproduction. Module sums,
origins and inventory hashes are retained. The executable's generic version
banner alone was not treated as source provenance. Initial unstripped and
stripped-with-build-ID attempts differed; ELF inspection identified the absent
build-ID sections, and the matching attempt is retained separately.

Both `remote_audit_owned_awg_alignment.py awg2_lab|awg31_lab` readbacks passed
all 11 comparisons: static/live server key, peer keys, port, address, S/H fields,
HP key, content padding and randomized trailers. Raw keys and configurations
were compared only in memory and never returned as evidence.

## Bidirectional execution

The existing `remote_run_owned_awg_core_interop.py` ran sequentially for both
profiles with the exact Core revision, confirmed `shrek` alias, strict SSH
trust and direct-default-route preflight. Each profile passed authenticated
TLS/HTTP at MTU 1280, 1400 and 1408; exact portable test binary hashes and
packet observations are in the receipt. Temporary remote files were removed.

| Server peer | RX increase | TX increase | New handshake |
| --- | ---: | ---: | --- |
| AWG2 | 22,236 bytes | 22,564 bytes | yes |
| AWG3.1 | 30,430 bytes | 26,941 bytes | yes |

The peer identity, server binary/processes, saved configuration and static
live configuration were unchanged. The live hash comparison excludes only
the learned peer Endpoint, which legitimately changes when the Pi connects.
No server configuration, key material, entitlement or route was changed by
these interop tests. No incompatible profile was relabelled: the current Core
tests `TestAWG2RejectsAWG31Fields`, `TestAWG31SubsetFailsClosedBeforeDeviceCreation`
and `TestAWG2SubsetFailsClosedBeforeDeviceCreation` also passed with `-tags with_awg`.

## SSH access and remaining work

The owner explicitly requested login and a new SSH key. A dedicated Ed25519
key was added; all previous authorized-key bytes were preserved. Both DE IPs
were authenticated and confirmed to identify the same machine. The earlier
interpretation of two different servers was disproved. `ssh pokrov-de` now
works with the new key and strict host checking. Local SSH config/trust backups
are retained; private keys are outside the repository and evidence.

There was no deployment, publication, signing or release-artifact change.
The full consolidated goal remains active. This is a bounded Pi lab exchange,
not mobile-provider breadth, a long soak or final installed-client acceptance.
The Windows W02 attempt was not executed because the guest was locked; the VM
was returned off/NIC none, host routes/DNS unchanged, and no backend switch ran.

Evidence and commands are retained in `E:/r12-a02-server-20260908` and
`E:/r12-de-access-20260908`. The initial interop invocation lacked its temp
directory and stopped before execution; the corrected run is retained separately.

## Receipt verification

- `python -B -m pytest -p no:cacheprovider tests/test_agent_docs_contract.py tests/test_agent_context_packet_audit.py -q`: 33 passed.
- `python -B scripts/agent_context_packet_audit.py --platform-context-root .`: PASS.
- `python -B scripts/check-links.py`: PASS for its public-link contract scope.
- Independent receipt readback: all 24 retained file sizes and SHA-256 hashes
  match; both interop results, all 22 alignment checks and both counter deltas
  pass. The register still contains 83 unique IDs.
- Scoped Markdown file links and `git diff --check`: PASS.
