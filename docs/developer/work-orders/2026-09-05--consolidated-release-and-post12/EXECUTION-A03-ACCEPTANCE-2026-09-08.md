# A03 — приёмка исходного Core/AWG-lab критерия

**VERIFIED / I4** для неизменённого [R12-A03 DoD](03_RELEASE_BACKLOG_83.md#r12-a03--cross-field-validation-и-packet-size-budget),
Core `02a091cb0e369192a5ad0909b56ccba8aa1dce17`, pin
`github.com/amnezia-vpn/amneziawg-go/v3 v3.1.20260814`, разрешённые AWG2/AWG3.1
labs. Это приёмка validator/packet-budget и exact-pin MTU scope. Полный
consolidated план, D02/N03/N08 и выпуск остаются открытыми.

[Аудит доказательств](evidence/a03-acceptance-20260908/acceptance.json) сверил
53 хеша: прежние raw logs, A02 server/source provenance, текущие AAR/DLL,
Core implementation/tests и прочитанные upstream sources. Текущий Core чистый;
изменений исходников, зависимостей или библиотеки этот шаг не вводит.

| Пункт исходного DoD | Проверенное основание |
| --- | --- |
| H/S/I/J и upstream ограничения | `contract.go`, `contract_test.go`, `cross_field_test.go`: disjoint uint32 H; S packet budget; bounded I tags/512 generated bytes и запрет IPC injection; J count/size/order. Pinned stopped-device UAPI fixture подтверждает отличие upstream H validation от отсутствующей upstream cross-field защиты S/timing |
| Обязательная HP | AWG3.1 требует 32-byte HP key и S1–S4 >=12. Невалидный key/малый S4 отклоняются до device creation. AWG2 отклоняет AWG3.1 поля; HP-off не добавлен как положительный случай |
| Суммарный размер и MTU | S1+148, S2+92, S3+64, S4+MTU+32 и Jmax ограничены минимумом managed 2016 и target MaxMessageSize. Exact boundary и one-byte-over проверены для обоих контрактов. Pinned padding ограничен inner MTU |
| Timing | Максимальный rekey раньше минимального reject; receive-refresh формула использует upstream нижние keepalive/retry bounds. Проверены inverted/zero interval и pinned defaults |
| Предсказуемый отказ unsupported | Отрицательные subset/cross-field cases до создания device; разрешены только MTU 1280/1400/1408. 1360/1420 не включены в положительную матрицу |
| Exact-pin MTU 1280/1400/1408 | Шесть retained Pi↔owned-server TLS/HTTP cases для того же Core `02a091c`; по каждому ненулевые TX/RX. Свежий server readback совпал по binaries, saved/static live config и peer hashes, поэтому source/server/material inputs этих результатов не изменились |
| Сначала negative test, затем доказанное исправление | Hash-verified `before.log`: шесть воспроизведённых FAIL; `after.log` и текущий focused run PASS. Исправление — прежний `02a091c`, новое не требовалось |
| DisableCookies/upstream refresh | По исходному DoD сохраняются в POST12-05; публичный AWG scope не менялся |

Текущий focused run: **8 tests / 35 subtests PASS**, exit 0; Go 1.26.8,
`engine/sing-box` в `E:/r12core-implementation`:

```powershell
& 'C:/Users/kiwun/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.8.windows-amd64/bin/go.exe' test -count=1 -tags with_awg -run 'Test(SyntheticDualStackFixtureAndMTUSweep|SyntheticAWG31FixtureAndIPC|AWG31SubsetFailsClosedBeforeDeviceCreation|AWG2RejectsAWG31Fields|AWG2SubsetFailsClosedBeforeDeviceCreation|AWG31CrossFieldRejectionBeforeDeviceCreation|ManagedAWGPacketBoundaryAndDefaultTimings|PinnedAWG31CrossFieldAcceptanceBoundary)$' -v ./protocol/awg
python -B E:/r12-a02-server-20260908/server-readback.py E:/r12-a03-acceptance-20260908/server-current.json
python -B E:/r12-a03-acceptance-20260908/verify-acceptance.py
```

Все три команды завершились с exit 0. [Receipt](evidence/a03-acceptance-20260908/receipt.json)
сохраняет raw logs/collector/result. Старые [cross-field](EXECUTION-A03-CROSSFIELD-2026-09-08.md)
и [A02](EXECUTION-A02-2026-09-08.md) receipts неизменны. Указанные в первом
cross-field отчёте ещё не выполненные server/installed checks позднее получили
свои результаты; это не изменение его исторического наблюдения.

Inner MTU и managed packet ceiling не доказывают отсутствие внешней IP
фрагментации на любом пути. Полная device/path MTU, DNS, UDP53 и IPv6 matrix
остаётся в R12-D02, routing truth — в N03/N08. Эти проверки не помечены PASS.
Применимость самого A03 взята из исходного DoD без изменения его текста.
Новый candidate, deploy, publication, production configuration change и
release authorization этим результатом не создаются.

Документационные проверки: 33 tests PASS, platform-context audit PASS,
package validator — 83 R12 IDs / 378 legacy IDs / 366 local links PASS;
`git diff --check` PASS. Только строка A03 изменила статус в реестре;
исходные 13 импортированных разделов и legacy ledger сохранили свои хеши.
