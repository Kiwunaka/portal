# N04 — current-source probe fencing verification

Outcome: **IMPLEMENTATION_VERIFIED / I3 for the local scenarios below**.
Full N04 remains active: exact Android package acceptance and its N03 dependency
are open. No new runtime defect was demonstrated; no product code was changed.

Source tuple: client `5188505323d4fed03806c78756ba4428608ebd3b`, Core
`02a091cb0e369192a5ad0909b56ccba8aa1dce17`, platform before this report
`004857df3fa15fa016865cd9aaacf289f2d39557`.
[Retained summary, test cases and log hashes](evidence/n04-local-20260908/summary.json).

## What was verified

- Core `daemon/selected_outbound_probe_test.go`: selection A changed to B
  cannot supply B's proof; direct/cyclic selections are rejected. A timed-out
  call returns false, its delayed success cannot settle the already pending B
  call, the timeout sentinel fails, and a fresh B success passes.
- Android `AndroidCoreEgressProbeTest`: two concurrent calls, for both endpoint
  and group APIs, keep their results separate. A success cannot settle B;
  missing/throwing APIs produce unavailable. This uses a synthetic Core object,
  not an installed AAR or real endpoint transport.
- Android `AndroidCoreOperationalEventsTest`: duplicate sequence, prior
  generation, previous attempt and previous run are rejected. A new run may
  restart its sequence. `AndroidRuntimeDispatchPolicyTest` confirms a held
  callback released after destroy cannot mutate state or submit to a stopped
  runtime executor. Fail-closed policy tests also passed.
- Source readback: `scheduleCoreEgressProbe` captures the session, health
  generation, target and server; retries are sequential. Both dispatch and
  result handling check ownership/generation. Endpoint Core results use a
  call-local channel and captured context; operational logs are diagnostic,
  not the host health result channel. These source observations do not extend
  the synthetic endpoint test into physical-device proof.

## Executed commands

From `E:/r12core-implementation/engine/sing-box`, Go 1.26.8:

```powershell
& 'C:/Users/kiwun/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.8.windows-amd64/bin/go.exe' test -count=1 -v ./common/urltest ./daemon ./experimental/libbox
$env:CC='C:/Users/kiwun/tools/llvm-mingw-20260616/bin/x86_64-w64-mingw32-gcc.exe'
$env:CGO_ENABLED='1'
& 'C:/Users/kiwun/go/pkg/mod/golang.org/toolchain@v0.0.1-go1.26.8.windows-amd64/bin/go.exe' test -race -count=1 ./common/urltest ./daemon ./experimental/libbox
```

Both exit 0: three packages PASS; ordinary verbose run has 19 top-level tests.
Race detector reported no race in these executed tests.

From `E:/r12client/apps/android_shell/android`:

```powershell
$env:JAVA_HOME='C:/Users/kiwun/tools/jdk/17.0.18'
./gradlew.bat :app:testDirectDebugUnitTest :app:testStoreDebugUnitTest --tests '*AndroidCoreEgressProbeTest' --tests '*AndroidCoreOperationalEventsTest' --tests '*AndroidRuntimeDispatchPolicyTest' --tests '*AndroidCoreEgressFailClosedPolicyTest' --rerun-tasks
```

Exit 0, 162 tasks executed. Gradle filters apply to the store task: direct
ran 188 tests in 33 suites; store ran 22 tests in four suites. Zero failures,
errors or skips in either. This is 210 executions, not 210 distinct test cases.
Compiler deprecation/flatDir warnings remain in the retained log.

Client `git diff --check` exit 0; `git diff --name-only -- artifacts/releases`
empty. Existing generated registrant line-ending changes remain unstaged.
Core has no source diff. No APK/EXE publication, signing, promotion, production
mutation or device/network test occurred in this verification.

Platform report checks: `python -B -m pytest -p no:cacheprovider
tests/test_agent_docs_contract.py tests/test_agent_context_packet_audit.py -q`
— 33 PASS, exit 0; `python -B scripts/agent_context_packet_audit.py
--platform-context-root .` — exit 0; `git diff --check` — exit 0.
Retained log attributes disable line-ending conversion to preserve recorded hashes.

## Remaining acceptance

The N04 inventory is now `NEEDS_RUNTIME_PROOF`, rather than an asserted missing
source fix. Exercise the exact new Android package through timeout/reconnect
and correlate the effective route as required by N03. Local tests alone do not
close that dependency or the release. A02 still needs independently confirmed
DE server identity before the pending server readback; it is a separate gate.

Rollback is limited to this report/registry commit; runtime bytes are unchanged.
