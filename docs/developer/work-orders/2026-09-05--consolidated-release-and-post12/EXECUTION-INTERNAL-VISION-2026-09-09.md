# Поставка обработки вложений поддержки

Backend `876e78da2ee5426a11d5aa74239fcff5efc9fa5d` (PR #246) установлен на Brain.
Из 204 runtime-файлов изменились четыре модуля поддержки. Схема, зависимости
и frontend-входы совпали с предыдущим backend `d3eba89`. Подписанное дерево
слияния совпало с проверенным исходником; оба обязательных PR checks и оба
checks после слияния завершились PASS.

Команда `python -B scripts/remote_deploy_brain_portal_code.py` с
`--brain-ip 82.21.114.104 --backup-retain-count 50 --restart portal-api,portal-helpbot`
и существующим закрытым password-файлом завершилась с кодом 0. Backup сохранён
в `/root/portal_bot.deploy-backups/20260909T064101Z-28716`. Readback всех 204
hash и API health — PASS. Обновлены только публичные build metadata; остальные
значения `.env` сохранены. Worker AWG и два других бота сохранили PID.
Последующее чтение после дополнительного перезапуска API снова подтвердило
204 совпадения; источник этого дополнительного перезапуска здесь не установлен.

Проверка в действующем каталоге `/root/portal_bot` использовала PNG и PDF
с искусственными данными: два вызова Vision и один вызов итогового ответа,
10,049 секунды. Распознаны TIMEOUT, красный круг и сумма 99 RUB; чувствительные
поля скрыты. БД и платёжный провайдер заменены фикстурами, клиенту сообщение
не отправлялось. Это проверка обработки файлов, а не реальной оплаты или
доставки ответа в Telegram. Последняя остаётся MANUAL_OWNER_TEST.

На окончательном исходнике focused suite: 99 PASS. Более ранний широкий
прогон 424 tests + 192 subtests относится к исходнику до последних двух
исправлений; он не выдаётся за проверку финального дерева.

[Receipt и сохранённые результаты](evidence/internal-vision-20260909/receipt.json)
содержат hashes исходных bytes и варианта CRLF→LF для Git. Операторские metadata
прочитаны временной bootstrap-сессией с logout; внешний OIDC этим не проверялся.
Релиз клиента и общий план остаются открытыми.
