# G01 scope record и M01 legal/seller review

Дата: 2026-09-06. Source baseline: `b40ab5f`.
Evidence: [evidence/g01-m01-scope-legal.json](evidence/g01-m01-scope-legal.json).

## G01 — VERIFIED / I3

Текущий запрос владельца — выполнить сводный план и принятые дополнения —
разрешает продолжать локальную реализацию в согласованных feature worktrees.
Он сохраняет отдельные разрешения для production/release, публикации и расходов.
Это текущая граница исполнения; ранний `RECONCILE_ONLY` в BASELINE описывает
стартовый этап и больше не ограничивает все локальные source changes.

Проверены все 83 постановки: у каждой есть исходная классификация характера
работы, owner role, применимость и Definition of Done. Обязательность берётся
из этой применимости и принятой release-последовательности; P0 чужой области
не активирует её автоматически. Все 378 legacy строк и их исходный ledger
сохранены. Проверка структуры не переносит их старые статусы на новые bytes
и не является повторным полным аудитом 378 требований.

Пути и роли репозиториев остаются прежними: platform/master, client/main,
Core и release-index — отдельные lanes. Фактическая локальная работа идёт в
platform `VPN-consolidated-plan-start`, client `E:/r12client`, Core
`E:/r12core-implementation`; исходные dirty checkouts остаются вне write set.
Linux/HAPP/новые transports, ATS/Broker/Smart Access и shortlinks сохраняются
в предусмотренной условной/postrelease очереди. Публичный scope, цены, route
defaults и security boundary не меняются без соответствующего решения.

Исторический OWNER-FREEZE и candidate.33 остаются сохранёнными. Freeze не
переименован в GO; новый candidate/release resume и Gate F не выданы. G01
закрыт только как требование к фиксации scope/authority, а не как release gate.
G03 и все остальные R12 оцениваются по собственным критериям.

## M01 — NEEDS_CONTEXT / I1

- Дата части 10.8 статьи 5: **01.09.2025**. Проверены текст пункта 1 статьи 1
  и специальная дата в части 2 статьи 2 закона № 332-ФЗ. Общая дата
  01.01.2026 относится к другим положениям. В backlog дата уже верна;
  правило и ссылки теперь внесены в канонический operational contract.
- Offer page содержит условия доступа и контакты, но не утверждённого продавца.
  `commercial-contract.json` сохраняет `blocked_unpublished`,
  `blocked_unapproved`, пустые allowed channels и `launch_ready=false`.
- Transparency page прямо сохраняет открытый вопрос реквизитов. Checkout
  обещает отправку кода и чека по email; текущий POKROV relay реализует письмо
  с access key и номером заказа. Это не доказательство выдачи чека провайдером.
  Реальный чек, provider setup и seller-to-receipt binding не обследованы.
- Нельзя вывести разрешённость own-site/lifecycle из наличия канала или
  согласия на рассылку. Канонический документ теперь явно требует отдельной
  актуальной оценки содержания, аудитории и канала перед внешним запуском.

Владельцу отправлен запрос утверждённого продавца и источника реквизитов/чека.
Ответа на момент этого среза нет. Публиковать выдуманные реквизиты, менять
legal flags или объявлять коммерческую готовность нельзя. User-facing offer,
checkout и receipt claims требуют завершения этой сверки; они не помечены PASS.

Правовая проверка ограничена указанной нормой и датой, не всей квалификацией
сервиса или каналов. Официальная карточка публикации закона найдена, но её
полный текст через web fetch недоступен; прочитан текст закона в КонсультантПлюс.
Актуальная оценка конкретного запуска и provider evidence остаются OPEN.

## Результат и границы

Runtime и product code не изменены. Канонический operational document,
scope record и реестр отражают текущую границу доказательств. Точные команды
и результаты проверок документации/marketing guards сохранены в JSON evidence.
Push, merge, deploy, новая сборка, отправка писем, оплата и публикация
не выполнялись. Общий release goal остаётся открытым.
