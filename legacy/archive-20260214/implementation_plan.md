# 🛡️ План: Озон как запасной вариант (Умная подписка)

## Цель
Сделать так, чтобы при обновлении подписки пользователь получал **ДВА** сервера в приложении:
1.  **Yahoo (Standard)** — текущий, основной.
2.  **Ozon (Backup)** — запасной, работает в РФ лучше при блокировках.

Оба сервера используют **один и тот же ключ**.

## 📋 Этапы реализации

### 1. Настройка сервера (Backend)
Настроить Inbound 4 (VLESS Reality) так, чтобы он принимал подключения и от "Yahoo", и от "Ozon".
- **Действие:** Обновить настройки Inbound #4 в БД.
- **SNI:** `["www.ozon.ru", "yahoo.com"]` (Порядок важен для панели, но для Бота - нет, так как он сам формирует ссылки).
- **Dest:** `www.ozon.ru:443`
- **Результат:** Сервер готов принимать оба варианта.

### 2. Перехват порта 2096 (Reverse Proxy)
Чтобы мы могли "подсунуть" юзеру умную подписку, нам нужно перехватить запрос к `/s8Kx2mP7qR4wT/...`.
- **Действие 1:** Перенести панель 3x-ui с порта `2096` на `2097`.
- **Действие 2:** Настроить Caddy на порту `2096`.
    - Запросы `/s8Kx2mP7qR4wT/*` -> слать на нашего бота (`localhost:8080`).
    - Остальные запросы -> слать на панель (`localhost:2097`).
- **Результат:** Ссылки у юзеров не меняются, но подписку теперь отдает Бот.

### 3. Доработка Бота (API)
Научить бота генерировать двойной конфиг.
- **Действие:** Добавить в `api.py` эндпоинт `GET /s8Kx2mP7qR4wT/{sub_token}`.
- **Логика:**
    1. Найти юзера по `sub_token` (или `tg_id`).
    2. Взять его ключ (UUID).
    3. Сгенерировать строку 1: `vless://...sni=yahoo.com...#Yahoo_Main`
    4. Сгенерировать строку 2: `vless://...sni=www.ozon.ru...#Ozon_Backup`
    5. Отдать обе строки текстом (base64 по требованию клиента, обычно просто текст работает).

## ⚠️ Откат (Если что-то пойдет не так)
1. Вернуть панель на порт 2096.
2. Убрать Caddy с 2096.
3. Всё работает как раньше (только одна подписка).
