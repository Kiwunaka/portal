# Legacy Scripts Archive

This folder contains older one-off scripts that were used during the initial single-server setup and early migrations.

They were moved here to reduce clutter in the repo root. New operational scripts live in `scripts/` and are referenced
from `docs/`.

Notes:
- These scripts are not maintained.
- Do not put secrets into the repo; keep credentials in untracked files or environment variables.

